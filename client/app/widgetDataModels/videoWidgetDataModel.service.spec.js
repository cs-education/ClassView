'use strict';

describe('Service: videoWidgetDataModel', function () {

  // load the service's module
  beforeEach(module('classViewApp'));

  // instantiate service
  var videoWidgetDataModel;
  beforeEach(inject(function (_videoWidgetDataModel_) {
    videoWidgetDataModel = _videoWidgetDataModel_;
  }));

  it('should do something', function () {
  });

});
